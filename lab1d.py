#!/usr/bin/env python3

# Define the integer values
x = 10
y = 2
z = 5

# Calculate the result
result = x + y * z

# Print the output
print(str(x) + " + " + str(y) + " * " + str(z) + " = " + str(result))
